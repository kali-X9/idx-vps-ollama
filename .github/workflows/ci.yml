name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        - name: Ensure Scripts Dependencies Installed + Test Run
####second next
Here is the complete correct version of your script, fixed and formatted:

---

```yaml
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install format/lint tools
        run: |
          sudo apt-get update && sudo apt-get install -y shellcheck shfmt

      - name: Format scripts using shfmt
        run: |
          shfmt -w -i 2 vm.sh vps.sh tools/ollama-install.sh

      - name: Perform static code analysis with Shellcheck
        run: |
          shellcheck -x vm.sh vps.sh isn//-------------------------------------------------Snapshot
